const rectangleWidth = 200
const rectangleLength = 100

const rectangleArea = rectangleLength * rectangleWidth

console.log(`${rectangleArea} = ${rectangleWidth} * ${rectangleLength}`);


const length = prompt("How much length ?");
const width = prompt("how much width ?");
const height = prompt("how much height ?");

const calc = length * width * height;
 alert(`The result from ${length} * ${width} * ${height} = ${calc}`);